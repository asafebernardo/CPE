/**
 * Default theme — the original RouterGui ACS dark look.
 * Re-exported here so every theme lives under `themes/<id>/theme.ts`.
 */
export { routerTheme as defaultTheme } from '../../routerTheme';
